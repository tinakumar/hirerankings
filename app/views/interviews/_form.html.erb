<%= form_for(@interview) do |f| %>
  <% if @interview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interview.errors.count, "error") %> prohibited this interview from being saved:</h2>

      <ul>
      <% @interview.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :question_id, Question.all.collect { |q| [q.body, q.id] } %><br>
  </div>
  <div class="field">
    <%= f.select :company_id, Company.all.collect { |c| [c.name, c.id] } %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
